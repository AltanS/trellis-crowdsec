[project]
name = "trellis-crowdsec"
version = "0.1.0"
description = "CrowdSec Security Engine role for Trellis WordPress deployments"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ansible-lint>=26.1.1",
]
